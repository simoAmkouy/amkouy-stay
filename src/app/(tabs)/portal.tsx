// Owner's primary tab — reuses the exact Owner Portal screen (Module 2/7/8/11) rather than a
// second copy. See src/constants/role-navigation.ts for why Owner gets one consolidated tab
// instead of splitting Properties/Reservations/Revenue/Contracts into separate tabs.
export { default } from './more/owner-portal';
