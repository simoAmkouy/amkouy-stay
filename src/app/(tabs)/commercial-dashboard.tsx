// Commercial Agent's primary tab — reuses the exact Commercial Dashboard screen (Module 10/11),
// not a second copy. Named differently from the staff `dashboard` tab since it's a distinct
// screen (`commercial_dashboard` resource, not `dashboard`).
export { default } from './more/commercial/dashboard';
